replicaCount: 1

image:
  repository: ghcr.io/noeljackson/av
  tag: "0.1.0"
  digest: ""
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  automount: true
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {}

service:
  type: ClusterIP
  port: 80
  targetPort: 14322

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: av.example.com
      paths:
        - path: /
          pathType: Prefix
  tls: []

credentialSecrets: []
# - name: av-basic
#   secretName: av-basic
#   mountPath: /var/run/av/basic

# Static mode is the portable, Git-managed configuration. Managed mode keeps
# the same immutable connector bootstrap here, while owner-managed policy and
# audit records live in an existing PostgreSQL database. AV never installs or
# creates that database or its Secret.
controlPlane:
  mode: static # static | managed
  existingDatabaseSecret:
    name: ""
    key: database-url
  # Exact OIDC `sub` for the first owner of an empty database. This is an
  # identifier, not a credential. It is ignored in static mode.
  initialOwnerOidcSubject: ""

config:
  listen: "0.0.0.0:14322"
  public_url: "https://av.example.com"
  auth:
    mode: oidc
    issuer: "https://zitadel.example.com"
    client_id: "replace-me"
    audiences: ["replace-me"]
    scopes: ["openid", "groups"]
    signing_algorithms: ["RS256"]
    allowed_groups: ["av-users"]
    group_claim: groups
    basic_users: []
  connectors: {}
  profiles: {}
  proxy_routes: {}
  max_connector_concurrency: 16
  api_rate_limit_per_second: 50
  api_rate_limit_burst: 100

resources:
  requests:
    cpu: 25m
    memory: 48Mi
  limits:
    cpu: 500m
    memory: 192Mi

podSecurityContext:
  runAsNonRoot: true
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop: ["ALL"]
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 65532

nodeSelector: {}
tolerations: []
affinity: {}
